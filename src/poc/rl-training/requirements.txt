# Achado por auditoria: sem teto de versao, um 'make venv-rl-training'
# futuro pode puxar uma major nunca testada aqui e quebrar silenciosamente.
# Teto = a major SEGUINTE a que ja roda neste venv hoje (gymnasium 1.3.0,
# numpy 2.5.3, stable-baselines3 2.9.0, torch 2.14.0, onnx 1.22.0,
# ipykernel 7.3.0) -- nao trava a versao exata (isto nao e' um lockfile, e'
# so um limite de major), mas impede o salto silencioso pra uma major
# desconhecida.
gymnasium>=0.29,<2
numpy>=1.24,<3
stable-baselines3>=2.0,<3
torch>=2.0,<3
# serve o .venv como kernel Jupyter selecionavel no VS Code (notebooks/) --
# nao precisa de 'jupyter' completo nem de registro manual de kernelspec, o
# VS Code detecta qualquer interprete com ipykernel instalado.
ipykernel>=6.29,<8
# necessario para tools/export_onnx.py -- tanto --random (onnx.helper/save)
# quanto --sb3 (torch.onnx.export usa o pacote onnx por baixo pra serializar
# o .onnx, mesmo no caminho --sb3 -- confirmado rodando, nao documentado em
# lugar nenhum da API do torch: falha em silencio de proposito escondida
# so aparece como "Module onnx is not installed!" na hora de exportar).
onnx>=1.15,<2
