- integração com o asa-engine
  - pré-requisito parcial já existe: src/node/ (runner headless, sem TUI, roda
    um cenário por argumento) -- falta a camada de rede/IPC de verdade com o
    asa-engine (split node/manager/handler)