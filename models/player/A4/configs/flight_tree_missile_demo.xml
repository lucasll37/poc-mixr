<!--
  Arvore de DEMO, para src/poc/dis/single-thread/configs/scenario_missile_demo.edl.in.

  Copia de flight_tree.xml (a arvore de PRODUCAO, intocada) com um ramo a
  mais dentro de 'engage_sequence': antes de reportar e evadir, tenta lancar
  um missil se houver arma disponivel (WorldView::weaponReady) e o contato
  estiver dentro do envelope (domain::LaunchPolicy).

  So falcon1 (o unico com 'stores:' no cenario de demo) usa esta arvore -
  os outros tres falcons continuam com o flight_tree.xml de producao. Em
  qualquer aviao SEM StoresMgr, LaunchEnvelope falha sempre (weaponReady fica
  false), entao o ramo extra e um no-op inerte - por isso e seguro esta
  arvore existir sem duplicar a producao byte a byte.

  Prioridade (Fallback, de cima para baixo) - identica a producao, so com
  o passo de lancamento inserido dentro do item 2:
    1) combustivel abaixo da reserva      -> volta para a base
    2) manobra de evasao valendo          -> tenta lancar, senao quebra e avisa
    3) alerta recebido de outro caca      -> vai apoiar no ponto avisado
    4) caso contrario                     -> patrulha

  So ha 1 missil no cenario de demo: depois do lancamento, weaponReady vira
  false e os ciclos seguintes caem direto em ReportAndEvade - a progressao
  "detecta -> lanca -> evade" acontece sozinha, sem estado extra na arvore.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Fallback name="root">

      <Sequence name="rtb_sequence">
        <FuelLow margin="0.05"/>
        <ReturnToBase/>
      </Sequence>

      <Sequence name="engage_sequence">
        <ContactDetected/>
        <Fallback name="engage_fallback">
          <Sequence name="launch_sequence">
            <LaunchEnvelope/>
            <LaunchMissile/>
          </Sequence>
          <ReportAndEvade/>
        </Fallback>
      </Sequence>

      <Sequence name="support_sequence">
        <AlertReceived/>
        <SupportAlert/>
      </Sequence>

      <Patrol/>

    </Fallback>
  </BehaviorTree>

  <TreeNodesModel>
        <Action ID="OnnxPolicy">
            <input_port name="label" type="std::string" default="ONNX">rotulo no dump e no quadro</input_port>
            <input_port name="normalized" type="bool" default="1">true: saida em [-1,1], desnormalizada aqui (export do SB3)</input_port>
            <input_port name="model" type="std::string">caminho do .onnx da politica</input_port>
        </Action>
        <Condition ID="OnnxScore">
            <input_port name="above" type="bool" default="1">true: SUCCESS se saida &gt; limiar; false: se saida &lt; limiar</input_port>
            <input_port name="index" type="int" default="0">qual saida do modelo comparar</input_port>
            <input_port name="threshold" type="double" default="0.500000">limiar de comparacao</input_port>
            <input_port name="model" type="std::string">caminho do .onnx (entrada float32[1,28], ver ObservationFields.hpp)</input_port>
        </Condition>
        <Action ID="SupportAlert"/>
        <Action ID="Navigate"/>
        <Action ID="LaunchMissile"/>
        <Condition ID="LaunchEnvelope"/>
        <Action ID="ReportAndEvade"/>
        <Action ID="ReturnToBase"/>
        <Action ID="PyDecide">
            <input_port name="label" type="std::string" default="PY">rotulo no dump e no quadro</input_port>
            <input_port name="script" type="std::string">caminho do .py que define decide(obs)</input_port>
        </Action>
        <Condition ID="FuelLow">
            <input_port name="margin" type="double" default="0.000000">margem somada a reserva de combustivel (fracao 0..1)</input_port>
        </Condition>
        <Condition ID="ContactDetected"/>
        <Action ID="Patrol"/>
        <Condition ID="AlertReceived"/>
    </TreeNodesModel>
</root>
